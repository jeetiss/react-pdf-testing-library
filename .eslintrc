{
  "extends": ["standard", "standard-jsx"],
  "ignorePatterns": ["assembly", "dist"],
  "overrides": [
    {
      "files": ["**/*.test.js"],
      "env": {
        "jest": true // now **/*.test.js files' env has both es6 *and* jest
      }
    }
  ]
}
